# RAT - Remote Access Tool (Private Setup)

Yeh ek single command wala tool hai jo Termux par bot ko automatically setup kar deta hai. 
**Privacy Lock:** Bot sirf usi Chat ID ko jawab deta hai jo aap setup ke waqt dalenge. Koi aur ise use nahi kar payega.

---

## 🚀 Perfect Command (Copy karein aur Termux mein paste karein):

```bash
pkg update -y && pkg upgrade -y && pkg install git -y && git clone https://github.com/vikramsengal/RAT.git && cd RAT && bash setup.sh
